# 59413_입양 시각 구하기(2)

- 0부터 23까지 HOUR을 지정해서 COUNT를 해야하는데, 특정 값은 COUNT가 0이라서 `GROUP BY`로 시간을 지정해도 잡히지 않는 시간대가 발생한다.
  - 이를 해결하기 위해서, 가상 테이블이 필요하다
  - 0부터 23까지 시간을 가진 가상테이블을 생성하고, 시간을 기준으로 JOIN을 수행하면 빈 테이블이 만들어진다.
    - 이후, 원하는 조건대로 결과가 발생하도록 수행하면 된다.