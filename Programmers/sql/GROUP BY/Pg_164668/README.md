# 164668_조건에 맞는 사용자와 총 거래금액 조회하기

- 먼저, 판매된 가격의 총 합이 70만원 이상이 되는 유저를 구해야 함 (서브쿼리)
  - HAVING을 활용해서 SUM(PRICE)가 700000이상이고, WHERE에서 STATUS가 DONE인 것만 체크하도록 설정
  - 이후, USED_GOODS_BOARD와 JOIN해서 하나의 테이블로 바꾸고, 원하는 값을 추출