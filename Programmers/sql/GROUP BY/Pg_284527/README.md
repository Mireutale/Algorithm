# 284527_조건에 맞는 사원 정보 조회하기

- LIMIT 을 활용해서, 1위 한명만을 보여줌
  - 단점, 점수가 동일한 사람이 있는 경우에도 1명만 나옴
  - 이 경우를 예방하기 위해서 RANK를 활용

- RANK() OVER(ORDER BY SUM(SCORE) DESC) AS RK -> 이런 식으로 코드를 활용해서 점수별로 RK를 따로 지정
  - 이후, RK를 생성한 테이블을 WITH으로 설정해서 실제 구문에 활용