# 299305_대장균들의 자식의 수 구하기

- INNER JOIN, LEFT JOIN에 대해 알아야 함.
  - INNER JOIN은 그냥 JOIN과 동일하고, 둘중에 하나라도 null값이 있다면 출력하지 않습니다.
  - LEFT JOIN의 경우, 왼쪽(처음에 오는 테이블)의 값을 기준으로, 오른쪽(다음에 오는 테이블)의 값이 null 이더라도 그냥 JOIN을 수행합니다.
  - 마찬가지로 RIGHT JOIN도 있음.