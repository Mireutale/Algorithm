# 151136, 평균 일일 대여 요금 구하기

- SELECT로 원하는 열 선택
- FROM으로 어떤 테이블 에서 값을 가져올지 선택
- WHERE을 설정해서 조건 설정
- AVG를 활용해서 평균값을 설정
- ROUND를 활용해서 반올림을 수행하고 `,`뒤 숫자를 통해서 소수 점 아래 몇 번째 자리에서 반올림할지 설정
    - 반올림하는 자리 -1을 하면 된다.