# 131537, 오프라인&온라인 판매 데이터 통합하기

- UNION을 활용해서 두개의 SELECT 문을 하나로 합칠 수 있다.
  - UNION의 경우, 중복을 제거한다.
  - UNION ALL의 경우, 중복을 제거하지 않고 그대로 두 테이블을 합친다.
  - UNION의 경우 JOIN과 다르게 사용하는 Column의 개수 및 이름이 동일해야 하고, 수직 확장된다.
    - JOIN의 경우 Column의 개수나 이름이 동일할 필요가 없고, 수평적으로 확장된다.
      - 각 테이블의 값을 곱해서 결과를 내는 형식이다.