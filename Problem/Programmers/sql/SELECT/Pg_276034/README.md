# 276034_조건에 맞는 개발자 찾기

- 서브 쿼리를 활용해서, Python과 C#의 스킬코드의 값을 합치면 몇인지 구하고
  - 이 합은 곧 비트연산에 사용시 Python 또는 C#을 가지고 있는지 아닌지 판별 가능
  - & 연산을 통해서 만약 값이 0이 되면 Python 또는 C#을 가지지 않고 있다고 판단
    - 값이 != 0인 모든 값을 출력하고 순서대로 정렬하면 해결